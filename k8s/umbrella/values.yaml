postgresql:
  auth:
    existingSecret: postgres
  fullnameOverride: postgres
  primary:
    persistence:
      storageClass: standard
      size: 5Gi
      accessModes:
        - ReadWriteOnce
    initdb:
      scriptsConfigMap: postgres-init
  volumePermissions:
    enabled: true
kafka:
  replicaCount: 1